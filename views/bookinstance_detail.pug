extends layout

block content

  h1 ID: #{bookinstance._id}
  
  p #[strong 書籍名:] 
    a(href=bookinstance.book.url) #{bookinstance.book.title}
  p #[strong 版:] #{bookinstance.imprint}

  p #[strong 状況:] 
    if bookinstance.status=='貸出可能'
      span.text-success #{bookinstance.status}
    else if bookinstance.status=='準備中'
      span.text-danger #{bookinstance.status}
    else
      span.text-warning #{bookinstance.status} 
      
  if bookinstance.status!='貸出可能'
    p #[strong 返却日:] #{bookinstance.due_back_formatted}

  hr
  p
    a(href=bookinstance.url+'/delete') 書籍現物削除
  p
    a(href=bookinstance.url+'/update') 書籍現物更新